{
  "name": "madyankin-name",
  "description": "Alexander Madyankin's Homepage",
  "version": "1.0.0",
  "author": "Alexander Madyankin <alexander@madyankin.name>",
  "private": true,
  "dependencies": {
    "gatsby": "^5.3.3",
    "gatsby-link": "^1.6.15",
    "gatsby-plugin-react-helmet": "^1.0.5",
    "gatsby-plugin-styled-components": "^1.0.5",
    "gatsby-plugin-typography": "^1.7.9"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --trailing-comma es5 --single-quote --write 'src/**/*.js'",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "prettier": "^1.6.1"
  }
}
